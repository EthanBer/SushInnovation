import React from 'react';
import { BrowserRouter as Router, Route, Routes, Link } from 'react-router-dom';
import Home from './pages/Home';
import About from './pages/About';
import NavigationBar from './pages/animated-search-nav.tsx'; // Adjust the path as necessary

const App = () => {
  return (
    <Router>
      <nav>
        <ul>
          <li><Link to="/">Home</Link></li>
          <li><Link to="/about">About</Link></li>
          <li><Link to="/animated-search-nav">Animated Search Nav</Link></li>
        </ul>
      </nav>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/animated-search-nav" element={<NavigationBar />} />
      </Routes>
    </Router>
  );
};

export default App; 